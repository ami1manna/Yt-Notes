import reducer from './inviteSlice';
import * as inviteThunks from './inviteThunks';
import * as inviteSelectors from './inviteSelector';

export { inviteThunks, inviteSelectors };
export default reducer;
